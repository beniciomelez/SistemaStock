<Window x:Class="SistemaStock.Views.Movimientos"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Movimientos"
        Width="1000"
        Height="600"
        WindowStartupLocation="CenterScreen">

    <Grid Background="#F5F5F5"
          Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- TÍTULO -->

        <StackPanel Grid.Row="0"
                    Margin="0,0,0,25">

            <TextBlock Text="Movimientos"
                       FontSize="32"
                       FontWeight="Bold"/>

            <TextBlock Text="Historial de entradas y salidas de productos"
                       FontSize="16"
                       Foreground="Gray"
                       Margin="0,5,0,0"/>

        </StackPanel>


        <!-- FILTROS -->

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,20">

            <TextBox x:Name="txtBuscarMovimiento"
                        Width="250"
                        Height="40"
                        FontSize="15"
                        Padding="10"
                        TextChanged="txtBuscarMovimiento_TextChanged"/>

            <ComboBox x:Name="cmbTipo"
          Width="150"
          Height="40"
          Margin="15,0,0,0"
          SelectionChanged="cmbTipo_SelectionChanged">

                <ComboBoxItem Content="Todos"/>
                <ComboBoxItem Content="Entrada"/>
                <ComboBoxItem Content="Salida"/>

            </ComboBox>

        </StackPanel>


        <!-- TABLA -->

        <DataGrid x:Name="TablaMovimientos"
          Grid.Row="2"
          AutoGenerateColumns="False"
          IsReadOnly="True">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Fecha"
                                    Binding="{Binding Fecha}"
                                    Width="150"/>

                <DataGridTextColumn Header="Producto"
                                    Binding="{Binding Producto}"
                                    Width="*"/>

                <DataGridTextColumn Header="Tipo"
                                    Binding="{Binding Tipo}"
                                    Width="120"/>

                <DataGridTextColumn Header="Cantidad"
                                    Binding="{Binding Cantidad}"
                                    Width="120"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</Window>